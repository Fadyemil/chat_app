import 'package:flutter/material.dart';

class GroubScreen extends StatelessWidget {
  const GroubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('groub-screen'),
    );
  }
}